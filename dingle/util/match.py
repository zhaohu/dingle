# -*- coding: utf-8 -*-
'''
Provide basic utility functions for matching queries.
'''


def match_query(dict_obj, query_obj):
    if isinstance(query_obj, dict):
       for (k, v in query_obj.items():
           
    return True
